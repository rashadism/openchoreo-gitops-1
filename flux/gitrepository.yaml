apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: openchoreo-gitops
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/rashadism/openchoreo-gitops-1
  ref:
    branch: main
  # Public repo — no secretRef needed. For a private repo, pre-create a Secret in
  # flux-system and reference it via:
  # secretRef:
  #   name: git-credentials
